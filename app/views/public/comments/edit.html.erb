<h2>コメント編集</h2>

<%= form_with model: [@restaurant, @review, @comment], local: true do |f| %>
  <div class="form-group">
    <%= f.label :body, "コメント" %>
    <%= f.text_area :body, rows: 4, class: "form-control" %>
  </div>
  <%= f.submit 'コメントを更新', class: "btn btn-primary" %>
<% end %>