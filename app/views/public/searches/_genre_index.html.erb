<div class="side-bar">
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>ジャンルから探す</th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, genre_search_path(genre_id: genre.id), class: 'text-dark' %></td>
        </tr>
      <% end%>
    </tbody>
  </table>
</div>